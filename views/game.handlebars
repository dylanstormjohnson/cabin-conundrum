<nav class='navBarContainer'>
  <h2>Cabin Conundrum</h2>
  <p>{{username}}</p>
  <a>restart</a>
  <a>high scores</a>
</nav>


<div class='gamePlayContainer'>
  <div class='roomContainer'>
    {{#each room as |room|}}
    <p>
      {{room.id}}
    </p>
  </div>
  <aside class='inventoryContainer, float-right, col-sm'>
    <ul>
      Available Items
      <li>
        {{inventory.id}}
        {{item_name}}
      </li>
      {{!-- add an item selector? --}}
    </ul>
  </aside>
</div>